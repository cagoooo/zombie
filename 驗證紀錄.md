## 2026-09-09 前景驗收追查更正

本節更正前次對隱藏事件的歸因，不代表 20 分鐘耐久通過。

- 受控 A/B：同一分頁使用 CDP Emulation.setFocusEmulationEnabled(true/false)，連續三組 true→false 都觀察到 focus／blur 與 visible／hidden 切換。最初一組關閉模擬僅觸發 blur，頁面仍 visible，證明失焦與可見性是不同訊號。
- 一般 locator.innerText 讀取後沒有新增焦點／可見性事件。Page.bringToFront 則出現 focused=true 但 visibility=hidden，不能把該指令成功視為真實前景成立。
- 因此，先前反覆「關閉焦點模擬以核對真實前景」本身可能觸發遊戲的 blur／visibilitychange 暫停；不能把這些片段全部歸因於使用者切換視窗，也不能將它們當成純粹的真實背景操作驗收。
- 另外觀察到一筆與當下切換指令不同時的 visibilitychange；其外部觸發來源未確認，不能聲稱所有中斷都已找到唯一原因。
- 遊戲現有 lifecycle 處理器符合預期，不修改或繞過自動暫停。後續流程：測試開始前一次性設定焦點模式、等待狀態穩定；計時期間只被動記錄 visibilitychange／blur／focus 與幀時間，不再切換焦點模擬、不以 bringToFront 作通過證據；若控制工具必須改變焦點，將該段另列人工干預並停止連續計時。
- 臨時事件監聽器已移除。20 分鐘連續真實前景與實體手機鎖屏仍待驗；既有 75 項程式測試結果不受此追查更正影響。

# v1.11.0 逐波分析驗收（2026-09-09）

- 75 項測試通過，新增 6 項逐波回歸。兩圖十波逐波數據與累計一致；每個準備階段做 JSON 匯出匯入往返，低於原有 64 KiB 上限。拒絕能源不平衡、重複波次、非法漏怪、缺省統計及超過累計的逐波來源。
- Chrome 本機正常資源工廠：兩座 Lv.1 電弧塔花完 300 起始能源，第一波 11 擊退；開波 0＋收入 207＋退款 0−支出 0＝結束 207。電弧有效扣血 704。暫停選波顯示一致，重新載入及繼續上次部署仍保留該波。
- Chrome 無防守前哨（1×→UI 2×）：第一波漏怪 basic=11，核心損失 88、基礎修復 5，核心 17、能源 375；第二波 runner=3，實際核心損失 17、lost。累計損失 105 包含前波曾修復的 5，並非扣血溢算。建議引用高速漏怪 3 與能源 375。
- RWD：320×640、390×844、844×390、768×1024，scrollWidth 等於 innerWidth、visualViewport.scale=1；選單字級 16px。390 直向戰報實際觸控上滑後 dialog.scrollTop=27，能點繼續；雙指測試 moving=true／shooting=true，touchCancel 後兩者皆 false。
- 背景驗收：CDP Emulation.setFocusEmulationEnabled(false) 解除工具強制前景後，建立另一測試分頁，實測 visibility=hidden、focused=false、paused=true、BGM paused=true、shooting=false、moving=false。隔開多次工具工作後再次讀取，敵人與戰報 JSON 完全一致，熱量與 EMP 冷卻不變。
- 返回限制：Page.bringToFront 與關閉第二測試分頁後，目標仍回報 hidden；未據此宣稱真實返回通過。為繼續 UI 功能測試恢復工具焦點模擬；此後取樣不作受控前景耐久證據。已發出使用者置前需求；未完成真實前景連續 20 分鐘及實體手機鎖屏恢復。
- 範圍：本批開發 N1＋N2，保留原戰鬥數值；沒有新增 N3 難度或 N4 獨立戰報匯出。現有續玩存檔包含已完成波次的紀錄，戰鬥中存檔仍回到最近準備階段。

# v1.10.0 戰況資訊驗收（2026-09-09）

- 命令：npm test 69/69、npm run build、npm run check:release、git diff --check 均 exit 0。Three chunk 622.29 KB 為既有建置警告，本批未增加依賴。
- 新增六項測試：兩圖二十波出生情報一致、扣血／破盾／過量傷害、武器／塔／EMP 歸因、實際漏怪／核心／修復、統計存檔往返與舊檔相容、blur／visibilitychange／pagehide 與時間步進暫停。
- Chrome 真實 UI 匯入 artifacts/transfer-fixtures/battle-info-9.json。此為 10,000 初始測試能源所建的六設施部署、wave=9，不屬正常資源通關，也沒有前九波歷史。第十波預覽 38 名，另標條件援軍 4 名。
- Boss HUD：裂核者出場 2710/2710、第一階段；半血後顯示第二階段與 1.1 秒倒數，後續為援軍已召喚／全部清除才能獲勝。戰局結算 won、42 擊退、核心 100。
- 結算來源：基座 1 電弧扣血 5616.16、破盾 454.2、25 擊退；基座 2 電漿扣血 6616.78、破盾 358.8、17 擊退。UI 四捨五入至一位小數，合計擊退 42；Chrome error logs=[]。
- 暫停前後兩次讀取敵人陣列及 report 完全一致，BGM paused=true，明確點繼續才推進。新版本更新通知也顯示 v1.10.0 與本批說明。
- Chrome 320×640、390×844、844×390、768×1024 的統計面板無水平溢出；橫向長表可捲動並點繼續。390 直向情報展開保留雙搖桿，844 橫向 Boss 戰況可閱讀；依觀察將 Boss 橫向位置再上移 42px，直向多保留 30px 小地圖空間。最終版勝利戰報已再次顯示 v1.10.0。
- P0-01 限制：工具建立／切換測試分頁後觀測 visibility 仍為 visible，故不算真實隱藏事件通過；離頁返回發生重新載入，可由檢查點續玩，並非 BFCache 原戰鬥恢復證據。事件處理器以單元事件驗證；手機鎖屏／Safari 恢復仍待實機。
- 本批未執行受控前景 20 分鐘、實體 iPhone／Android 或真人平衡，不重複宣稱前批未完成項目已通過。

# 驗證紀錄

## v1.9.1 批次 D：戰局驗收與資源修正（2026-09-08）

- Chrome 實際操作兩圖各兩套護盾／修復配置十波通關：工廠各 249、前哨各 245 擊退，核心皆 100%；觀察裂核者第二階段召喚與完整結算。前哨修復局含一次準備檢查點重新載入。
- 無防守在第二波正常失敗；勝敗後能重開。手機雙指移動／磁軌射擊、取消觸控、快速連點、設定返回與四尺寸比例／溢出檢查通過（Chrome 模擬）。
- 修正冰凍塔水晶幾何逐次新建問題，改共用快取；50 次程式重建與 Chrome 相同條件重建確認不累積。加入固定 300 筆幀時間診斷，修正戰局腳本的瀏覽器與分圖座標。
- 63 項測試通過。詳細結果與限速片段見 [驗收報告-v1.9.1.md](驗收報告-v1.9.1.md)。
- 待驗：受控全程前景 20 分鐘效能、真實分頁隱藏／恢復、手機實機與真人平衡；不把混合長時間操作當成全部耐久通過。


## v1.9.0 批次 C：冷卻工廠與新敵人（2026-09-08）

- 設定可切換「能源前哨／冷卻工廠」；準備階段保存目前部署後切換，兩圖各自保留檢查點。舊版 JSON 相容，跨圖匯入先預覽、確認後重新載入對應地圖。
- 第五把磁軌狙擊槍：傷害 95、間隔 1.1 秒、熱量 32、射程 30，同射線最多兩敵，第二敵 65% 傷害，障礙物可阻擋。
- 工廠第 3 波起衝刺感染者：接近玩家預警 0.8 秒、沿路衝刺 1.2 秒；冰凍與 EMP 能中止。第 6 波起護盾感染者，電弧／EMP 加倍剝盾。
- 工廠第 10 波裂核者半血後加速，預警 1.2 秒後一次召喚四名援軍；必須處理全部敵人才勝利。第一張地圖保留原有敵人配置。
- Blender MCP 原創四件 GLB，合計 444 三角面、65,028 bytes；保留 .blend、生成腳本、CC0 與 SHA256。三款敵人各有兩段腿部動畫。
- 60 項測試通過；兩圖各兩套正常資源配置十波「邏輯模擬」通關，無防守正常失敗。Chrome 抽驗切圖、基座座標、第六波新敵人、磁軌觸控連射、EMP 與 20 件素材載入；320／390／844／768 四尺寸無水平溢出。
- Chrome 抓到新增磁軌未配置瞄準幾何造成渲染中斷，已補上並新增五武器幾何回歸測試；重測場景無缺失幾何、渲染幀持續增加。
- 待驗：完整 Chrome 十波操作、Boss 第二階段視覺實戰、20 分鐘耐久、iPhone／Android 實機與真人平衡。上述未以邏輯測試代替。


## v1.8.0 支援設施與分支

- 53 項 Node 測試：護盾上限／傷害溢出／出售／戰鬥重建不刷護盾、修復上限／清波單次、八分支預覽與實際裝甲傷害／慢速／冷卻、付費與鎖定、舊 Lv.3 遷移、非法分支／護盾拒絕、兩套支援配置十波與純支援失敗；模型 SHA256、單場景與三角面均通過。
- Chrome 使用匯入的第 5 波準備測試部署（非正常完整戰局）：護盾 20／20、額外修復 4，16 項戰場素材全部載入且 assetErrors=[]。脈衝 Lv.2 選重擊預覽 79.4 傷害／1.73 次每秒，從 1850 扣 140 至 1710，Lv.3 分支 heavy 鎖定；出售退款 217 至 1927。
- Chrome 320×640、390×844、844×390、768×1024：scrollWidth 等於 innerWidth、scale=1、六張塔卡；橫向面板可捲到出售並完成操作。塔操作按鈕原高 29px，修正為最少 44px。
- 支援站面板複驗確認沒有升級／策略選項、出售按鈕高 44px。修正護盾站退款文案因 `180 * .7` 浮點誤差顯示 125 的問題，統一使用 `180 * 70 / 100` 顯示 126；實際退款原本即為 126。
- Blender MCP 原生 execute_blender_code 匯出兩件模型；獨立背景 Blender 封存母檔。後者背景模式顯示 MCP 不啟動屬預期，不當作建模 MCP 失敗；建模工具前一步已有成功輸出。
- 待驗範圍：新分支完整 Chrome 十波、真實 20 分鐘耐久與手機實機；正常資源十波結果為 Node 遊戲邏輯模擬。

## v1.7.0 存檔搬移與 EMP（2026-09-08）

- `npm test`：47／47，含舊檢查點相容、匯出入往返、非法包裝／地圖／冷卻／JSON／超大檔、EMP 裝甲／Boss／範圍邊界、重複獎勵、暫停冷卻、取消與備份還原、儲存失敗、非同步讀檔取消。既有正常資源十波邏輯測試通過。
- 可見 Chrome 本機預覽（127.0.0.1:4173）：真實選檔、取消後能源 300 不變；確認有效存檔後能源 550、1 座電弧塔、EMP 20 秒；備份還原後回能源 300、0 塔。非法 JSON 與 65,537 bytes 檔案顯示中文拒絕提示，戰局不變。
- Chrome EMP 操作：以匯入的測試部署移到路線旁，第一波按 EMP，4 名普通敵人由 64 HP 降至 19 HP；範圍外敵人維持 64 HP。暫停後冷卻 44.25 秒，跨四尺寸檢查維持同值。
- Chrome 320×640、390×844、844×390、768×1024：根頁寬度均等於視窗寬、scale=1；EMP 高 44px。發現並修正新存檔按鈕白底文字對比；複驗文字 rgb(237,244,223)、底色 rgb(32,48,31)，檔案控制項高 44px。測試後已還原 viewport。
- 新版重新載入後 Chrome error 紀錄為空、頁尾 v1.7.0。診斷期間曾有一筆人工讀取表達式括號錯誤，屬測試指令，不是遊戲程式例外。
- 實際下載驗收已補齊：使用者提供 deadzone-wave-1.json（381 bytes）。以正式解析器讀取及再次匯出／匯入逐欄比對 PASS；Chrome 選取該實際檔案，預覽為第 1 波準備、核心 100%、能源 300、0 塔、EMP 0 秒，取消後保留原戰局。先前下載事件逾時屬工具觀測限制，使用者提供檔案後已完成驗收。
- GitHub Pages 部署 34192576931 成功，程式提交 d7cf552；正式站 version.json 顯示 1.7.0、JS／CSS HTTP 200，Chrome 可開啟新版存檔設定。
- 建置與發布檢查通過；Three.js 約 622 KB 主套件的既有建置提醒仍在。此次沒有新增第三方套件。
- 未驗收：新功能實體 iPhone／Android、真實 20 分鐘連玩發熱／記憶體趨勢、新版完整 Chrome 十波與兩地圖平衡矩陣；先前 v1.6.0 完整瀏覽器戰局紀錄不可代替本版驗收。

## v1.6.0 R2：塔目標策略與升級預覽

本輪結果：42 項測試、五尺寸 R2 操作、四尺寸觸控回歸通過。原配置與四塔混合策略配置均完成十波、245 擊退、核心 100%，勝敗重開正常，瀏覽器 errors=[]。

本輪完成使用者選定的 R2 子項：四種塔各自可選「最前方／最近／最強」。最前方依路線進度；最近依塔與敵人距離；最強依目前生命值。只選射程內存活敵人，同分依前方優先、再依敵人 ID 決定，切換不扣款或重置冷卻。

升級面板比較目前／下一級的單次減傷前傷害、射程與每秒射擊次數，與戰鬥／射程圈共用公式；數值四捨五入顯示。同步顯示費用、剩餘或不足能源，Lv.3 停止升級；暫停與終局禁止修改。爆破半徑、減速時間、既有升級倍率與退款比例維持不變。

存檔保留各塔策略，舊版缺省策略補為最前方，未知策略拒絕載入。準備階段會保存；戰鬥中重整仍回到該波開始前的部署。本次沒有擴大為分支升級、難度選單或新增地圖。

驗證腳本：`npm test`（42 項）；`scripts/verify-tower-strategy.mjs` 驗證五種尺寸的策略切換、保存重載、升級預覽、能源不足、出售退款與返回戰鬥；`scripts/verify-full-session.mjs` 驗證正常資源十波與勝敗重開。自動化採 Edge，手機實機與新策略的主觀平衡仍待體驗。

## v1.5.0 電弧攻防首批（2026-09-08）

最新使用者要求優先以 Blender MCP 製作武器、敵人及攻防設施；本批已完成下列可玩範圍，先前 R3 最後的排序由這次明確要求局部調整。

- **新武器：電弧抑制槍。** 鍵盤 4／手機底部電弧鍵裝備；傷害 32、冷卻 0.45 秒、熱量 18、射程 21、範圍半徑 1.8，無視裝甲並減速 0.8 秒。保留共用熱量、射線遮擋與朝前射擊。
- **新敵人：裝甲感染者。** 第 4 波起按固定出怪規則混入；一般攻擊減傷 40%，電弧不受減傷；抵達核心扣 12 血。敵人仍只攻核心，十波總數仍為 245。
- **新設施：電弧干擾塔。** 150 能源、基礎傷害 18、射程 7.5；可部署、升級至 Lv.3、出售及檢查點還原，範圍電弧兼具克制裝甲與短暫減速。
- **Blender MCP：** 已確認原生工具連線成功，在獨立場景建模及輸出三個 GLB；合計 89,208 bytes、900 三角面。武器含 Grip／Muzzle，敵人含兩條腿部動畫。附原始程序、CC0 授權、摘要清單與獨立 .blend 可編輯封存。既有校園場景保留。
- **介面：** 四槍／四塔選單、第四波克制提示；修正橫向手機建造面板遮住「返回戰鬥」的問題。
- **本機驗收：** 38 項測試、建置、發布檢查；五尺寸新裝備專項、四尺寸觸控防縮放、十波 245 擊退／核心 100%、勝敗重開與 10 項一般瀏覽器驗證均通過。自動化使用 Edge 模擬；本版新增內容尚未完成手機實機驗收。
- **後續保留：** 更多武器／Boss、修復或護盾設施、塔目標策略、第二地圖、存檔匯出入及主動技能。此批不代表整份 R2～R7／R3 已全部完成；新槍暫無 Polar 外觀變體。

日期：2026-09-08

## v1.4.2：鎖定手機 viewport

- 使用者回報手機快速點擊仍會放大且無法縮回。根因是上一版只在部分元素設定 `touch-action`，viewport meta 沒有明確限制最大比例；部分瀏覽器仍把連點視為雙擊縮放。
- 修正為 `initial-scale=1`、`minimum-scale=1`、`maximum-scale=1`、`user-scalable=no`，根頁面使用 `touch-action: manipulation`；iOS gesture 事件及雙指 touchmove 另行 `preventDefault`。既有「恢復原比例」按鈕保留給已在舊版放大的分頁。
- 新增 `scripts/verify-mobile-fire.mjs` viewport 鎖定回歸：390×844、844×390、320×640、768×1024 皆確認 meta 設定、快速連點後 scale=1，CDP 嘗試強制 scale=2 仍維持 1；中心按鈕射擊、移動轉向、取消停火通過，例外 0。
- `npm test`：33／33；`npm run build`、`npm run check:release`：exit 0。實體 Android／iPhone、Safari 仍需在使用者裝置確認，瀏覽器或系統無障礙強制縮放不宣稱可由網站完全覆蓋。

## v1.4.1：手機誤縮放與朝向射擊

- 根因：僅搖桿／canvas 限制手勢，HUD 等區域仍可被瀏覽器縮放；右搖桿按中心沿用舊瞄準點，Game.fire 只搜尋終點附近而漏掉射線中段。
- 修正：根容器保留捲動、限制縮放，Safari gesture 防護；欄位字級至少 16px。已放大時顯示「恢復原比例」，暫時收束 viewport 範圍後還原原設定；提示獨立於戰場，依 visualViewport 調整可見寬度與位置。
- 左搖桿轉向／移動；右下角點按立即單發、按住連射、放開／取消停止，拖動右按鈕不再瞄準。前向射線優先最近敵人，排除後方／側方／超射程／障礙遮擋；桌機仍採原本滑鼠目標點。
- npm test 33／33；生產建置與發布檢查通過。新回歸案例覆蓋三槍前向命中及遮擋；既有桌機瞄準測試保持通過。
- verify-mobile-fire.mjs 四尺寸 390×844、844×390、320×640、768×1024：中心射擊、固定朝向、移動轉向、觸控取消、HUD 雙指／連點後 scale=1；用 CDP 強制 scale=2 後真正 tap「恢復原比例」可回 1，例外 0。純模擬縮小手勢未成功，不把它列為已驗證；恢復按鈕已驗證。
- verify-third-person.mjs 七組與 verify-release-browser.mjs 六組／七尺寸通過。實體 Safari／Android 與使用者原裝置仍待回測。
- 手勢語義參考 [MDN touch-action](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/touch-action)。

## v1.4.0：方案 C 首批與 Blender MCP

- 使用者確認既有建議並追加 MCP 安裝要求。守衛與三把槍各兩款（含原版），四個新 GLB 合計 1,189,008 bytes；八筆素材 ID／版本／來源／動畫／貼圖尺寸／三角面／SHA256 已保存。
- `npm test`：32／32；`npm run build`、`npm run check:release`：exit 0，Three.js 大模組提醒仍在。
- `verify-cosmetics.mjs`：八組通過，例外 0。包含預覽取消、四項套用與保存、缺檔重試、已存外觀全缺檔仍可開始、慢回應不覆蓋最後選擇、50 次換裝、五種手機平板尺寸。額外驗證 GLTFLoader 的 Middle1.L → Middle1L，移動時手部位置確實帶動槍械。
- 外觀量測在人物移動後同場景：低畫質原版 93／極地 95 次繪製，中／高 167／171；50 次切換後回到幾何 65／貼圖 13。與舊版初始空場數值不可直接混比；此項未量測實體手機 FPS、監聽器數或長時間發熱。
- `verify-full-session.mjs`：固定生產預覽 4173，全套極地外觀以一般按鈕／滑鼠及加速瀏覽器時鐘完成十波、245 擊退、核心 100%；第二局失敗、勝敗重開與第三局起始通過。勝敗重開同為幾何 95／貼圖 18、活動特效 0，例外 0。第一次開發網址測試在第九波被編輯觸發 HMR 中斷，已改用固定建置重跑通過。
- `verify-ab-foundation.mjs` 七組通過：補測原模型缺檔仍可由外觀介面恢復備援並開局。生產 SW 會接管請求，因此故障注入情境明確阻擋 SW，確保 Playwright 路由真的能攔截貼圖；PWA 本身另由獨立腳本驗收。
- `verify-third-person.mjs` 七組、`verify-bgm.mjs` 七組通過；保留雙指移動／射擊、續玩及實際音樂播放。`verify-release-browser.mjs` 六組含七尺寸、更新通知與缺失 chunk 恢復通過，例外 0。
- Blender 5.2.1 LTS＋Blender MCP 1.9.1：真實 stdio MCP SDK 列出 28 個工具，讀取場景、匯入守衛、保存母檔、修改電漿發光材質、重新匯出遊戲 GLB、取得視窗截圖。僅代表已執行的核心工具成功；28 個工具沒有全部測過。Codex 原生工具須重開後載入，目前尚未驗證該熱啟動後狀態。
- 詳細 MCP 安裝、固定來源 commit、連線與圖像證據見 [Blender MCP 安裝與驗證](Blender-MCP安裝與驗證.md)。實體 Android／iPhone／Safari、真人聽感與逐幀手指持握精修仍待驗收。
- 正式部署：功能提交 `4089d45`；Actions [34017754177](https://github.com/cagoooo/zombie/actions/runs/34017754177) success，公開 build `1.4.0-d81d15140dfe`。`verify-live-release.mjs` exit 0：18 個公開資源 HTTP 200，四個 GLB 的 SHA256 與清單一致；正式站八項外觀選擇／四項極地套用、開波、BGM、390×844／1024×768 操作及頁尾通過，例外 0。OG 圖摘要維持一致。截圖 `artifacts/live-cosmetics.png`。

## v1.3.0：實際 BGM 與方案 C 規劃

- 已確認舊版 GameAudio 只有 55／82.4 Hz 振盪器環境底音，沒有音樂檔；本輪新增 Urgent MP3 與單一播放元素、Web Audio 獨立 gain、循環、靜音偏好、首次開波啟用、暫停續播與錯誤重試。
- 原始 2,275,786 bytes → 遊戲檔 911,194 bytes；FFprobe／FFmpeg 完整解碼 exit 0。瀏覽器解碼長度 56.894694 秒，播放時間實際前進；響度與 SHA256 見 ASSET_SOURCES.md。
- `npm test`：29／29 通過，含 4 項音樂生命週期測試。`npm run build`、`node scripts/check-release.mjs`：exit 0；Three.js 大模組提醒仍在。
- `node scripts/verify-bgm.mjs`：7 組通過、pageerror 0。涵蓋首次手勢播放、暫停／續播、靜音重載與音量、曲尾循環、實際 MP3 請求阻擋後重試、五尺寸觸控設定；分頁可見性使用事件與 hidden 狀態模擬，不宣稱實體裝置驗收。
- 另以可見 Edge 視窗確認 MP3 播放時間前進；該自動化環境的另一個頁面並未使 document.hidden 改變，因此不把這次切頁嘗試列為通過的真實分頁測試。
- `node scripts/verify-ab-foundation.mjs`：6 組通過、例外 0；新音樂欄位與原本音效／環境聲設定保存相容。
- `node scripts/verify-release-browser.mjs`：SW 更新生命週期、chunk 恢復及七種 RWD 尺寸通過，例外 0。
- `node scripts/verify-full-session.mjs`：10 波、245 擊退、核心 100%；無防禦第 2 波失敗，勝敗重開及第三局開局通過；例外 0。
- 本機舊 5173 程序未載入新增 Vite define，報 `__BUILD_META__ is not defined`；確認 PID 與 H:/zombie Vite 路徑後重啟，再完成上述測試。這是本機舊伺服器設定狀態，不冒稱為 3D 渲染器 BUG。
- 方案 C 規格與四階段文件已建立；首批外觀組合尚待使用者選擇，`status: draft`，未把新 SKIN 寫成已完成。
- GitHub 提交 `c113cb3`，Actions [34015663843](https://github.com/cagoooo/zombie/actions/runs/34015663843) success；公開 `version.json` 為 `1.3.0-d25c00825509`。
- `node scripts/verify-live-release.mjs` exit 0：14 個公開資源 HTTP 200，MP3 為 audio/mp3、911,194 bytes；公開網址實際播放 `currentTime > 0.3`、長度 56.894694 秒；11 模型、3 武器預覽、桌機及手機／平板開波切槍、SW scope 正確，pageerror 0。報告 `artifacts/live-release-report.json`。
- Chrome 既有站點 SW 顯示 v1.3.0 新版提示，點擊重新整理後載入新版；保留準備階段檢查點。
- 實體 Android／iPhone／Safari、主觀配樂聽感及完整離線音樂仍未驗收／納入。

## 本輪更新：自動完整遊玩與 BUG 回歸

使用者追加授權後，已直接修正低幀率時間截短與貼圖依賴錯誤未納入就緒判斷。下列命令全部完成且退出碼為 0：

- `npm test`：**17／17**，包括原有 13 項及新增 4 項固定步長、射速、暫停／2×、卡頓上限測試。
- `npm run build`：成功；以下瀏覽器回測使用建置後的 `http://127.0.0.1:4173`。
- `node scripts/verify-browser.mjs`：原有 **10／10** 操作檢查通過，正常資源載入錯誤與 JavaScript 錯誤為 0。
- `node scripts/verify-full-session.mjs`：正常滑鼠／按鈕操作搭配加速瀏覽器時鐘，完成 **10 波、245 隻擊退、核心 100%**；不建塔不射擊的流程在第 2 波正常失敗；勝利重開、失敗重開及第三局啟動皆通過。未改遊戲資源或直接清除敵人。
- `node scripts/verify-asset-fallback.mjs`：在隔離測試頁阻擋 `colormap.png`，`assetsReady=false` 且 `assetErrors` 正確記錄該貼圖；幾何槍／SVG 備援、切槍與開波有效，沒有未處理的 JavaScript 錯誤。網路錯誤是本測試刻意觸發的情境。

新增證據：`artifacts/full-session-report.json`、`artifacts/asset-fallback-report.json`、`artifacts/full-victory.png`、`artifacts/full-defeat.png`。修正前查核保留在 `artifacts/planning-audit.json`，不能將其舊數據當成目前尚未修復。

本輪仍未在實體手機驗證；加速瀏覽器時鐘的自動操作不等同真人試玩或實機 FPS 量測。完整流程腳本已可在後續改版直接重跑。

以下保留原始版本的驗證紀錄，原先的 13 項測試及第一波驗證已由上方新結果擴充。

## 遊戲邏輯

`npm test`：13 項測試，涵蓋路徑、建造扣款、升級及退款、暫停、切槍冷卻、過熱、電漿範圍傷害、冰霜減速、擊退獎勵、防禦塔自主射擊、核心失守、巨型殭屍、通關與重設。

完整防守模擬只使用遊戲正常提供的建造、升級、開火與波次 API，未額外增加能源或直接殺死敵人。結果：第 10 波獲勝、擊退 245、核心 100%，遊戲模擬時間 206.2 秒。此為自動瞄準的邏輯驗證，不代表真人遊玩的難度或通關時間。

## 真實瀏覽器

`node scripts/verify-browser.mjs`：使用本機 Microsoft Edge 的 headless 模式。

- 桌面 1440 × 980：模型及貼圖、3D 武器預覽、建造／升級／出售、數字鍵與武器卡切換、暫停、連續射擊與擊退、速度及音效開關均通過。
- 第一波在實際 requestAnimationFrame 迴圈下完成，成功進入第二波。
- 手機模擬 390 × 844：沒有橫向溢出，觸控建造、武器切換、說明與恢復有效。
- 10 組整合檢查通過；JavaScript 與資源載入錯誤數為 0。
- 音效驗證涵蓋控制與 API 無錯誤；未宣稱完成主觀聽感測試。
- 行動裝置使用 Edge 觸控模擬，尚未在實體手機驗證效能。

原始報告：`artifacts/browser-report.json`；截圖：`artifacts/desktop.png`、`artifacts/combat.png`、`artifacts/mobile.png`。

## 靜態建置

`npm run build` 成功，靜態輸出在 `dist/`。Three.js 引擎模組約 619 kB（gzip 約 157 kB），Vite 有超過 500 kB 的體積提示，屬建置提醒，不是編譯錯誤。

尚未部署外部網站；可先透過本機啟動程式遊玩。


## A＋B／Blender 本輪回歸（2026-09-06）

- Blender 5.2.1 LTS 背景執行成功（exit 0），生成 MK2 .blend／GLB，542 三角面、3 網格、內嵌貼圖；見 `artifacts/blender-build-report.json`。
- 正常啟動載入 11 個素材模型與 3 張武器預覽，無 JavaScript 例外。守衛僅載入，尚未實作可走動角色。
- 10 組瀏覽器整合回歸通過；完整戰局第 10 波勝利、245 擊退、核心 100%；第 2 波無防禦失敗、勝敗重開及第三局起始通過。
- `artifacts/ab-foundation-report.json`：6 組新增驗證通過；阻擋外部色盤後兩把原始槍顯示備援、MK2 內嵌貼圖仍正常，個別及全部重試恢復。
- 設定中的 Esc 焦點 BUG 已由測試重現並修正；驗證音量／減少動態／瞄準輔助保存，關閉設定恢復原暫停狀態。
- 新增命中輔助與致命傷事件回歸；全套 19 項單元測試。
- 未執行 Godot 安裝或效能比較；Godot 文件是研究提案。真實手機效能、主觀聽感與第三人稱新操作仍未驗收。


## 直接開啟 HTML 的顯示問題（2026-09-06）

- 原因：以 `file:///H:/zombie/index.html` 開啟 Vite 原始入口，模組及透過模組匯入的 CSS 無法正常載入；並非已確認的 WebGL 渲染故障。
- 修正：直接開檔時顯示獨立的啟動說明與本機遊戲連結，避免出現無樣式操作畫面。正常入口為啟動視窗的 HTTP Local 網址，目前是 `http://127.0.0.1:5173/`。
- 驗證：從直接開檔提示點入 HTTP，11 個素材模型、3 張武器預覽正常，開波／暫停／恢復通過，JavaScript 例外 0；建置成功。證據：`artifacts/render-recovery-report.json`、`artifacts/http-render-recovery.png`。

## v1.1.0：A＋B＋P1-04／09 已確認實作（2026-09-06）

本節取代前面歷史段落中「守衛僅載入／第三人稱尚未實作」的現況描述。歷史紀錄保留，不視為本輪結果。G-A、斜俯視跟隨、敵人只攻核心已由使用者確認。

- 程式：WASD／Shift、雙搖桿多指、建造抽屜、常駐切槍、縮圖、主要障礙碰撞、隨角色射擊與射程、三種準星、待機／走跑與腳步、五步教學、畫質及檢查點。
- 版本：package.json 與 package-lock.json 均為 1.1.0；npm test 24／24（exit 0），npm run build（exit 0）。
- 最終實作後的完整戰局：正常按鈕／滑鼠、加速瀏覽器時鐘；10 波通關、245 擊退、核心 100%；無防禦第 2 波核心歸零；勝敗各重開及第三局起始通過。報告 full-session-report.json，JavaScript／非預期資源錯誤 0。
- 完整戰局重開資源：勝利重開與失敗重開皆為幾何 80、貼圖 13、活動特效 0；7 類特效池各 1～3 個，未超過每類 12 個上限。只證明本次流程計數一致，不代表全面長時間洩漏驗收。
- 新控制與續玩：third-person-report.json 7 組通過，errors=[]。390×844／844×390 的 CDP 多指觸控模擬可同時移動與射擊，取消後停止；切槍與暫停不捲頁；建造抽屜、教學重看／略過、暫停不移動正常。
- 檢查點：建塔／升級／切槍／人物位置重整一致；戰鬥重整回開波前，不重複領波次獎勵；不相容版本可新局。單元測試另含損毀 JSON、越界、非有限值、重複基座、錯誤投入及未知種類。
- 素材與設定：ab-foundation-report.json 6 組通過。故障注入刻意中斷外部色盤，驗證兩槍錯誤、內嵌貼圖 MK2 正常、逐項／全部重試、備援開波、設定保存和暫停恢復。
- 擴大故障：quality-fallback-report.json 3 組通過，errors=[]。阻擋單守衛／整模型目錄，各列 1／11 項失敗，均可備援開波；網路恢復後重載正常。
- 畫質：靜態網格 183→14。初始無塔無敵空場，Edge 152.0.4191.66 headless、1440×980 @1、每檔 30 幀暖機＋180 幀真實 rAF；低繪製 87、中／高 161；中位 16.7 ms、P95 16.8 ms，高檔最長一次 49.8 ms。低→中→高→低後幾何 50、貼圖 8，與首次低檔一致。
- 另一個有塔部署場景低 98／高 177 次，見 third-person-report.json；兩個場景不合併解讀。低／高差異主要是陰影通道，像素比及陰影尺寸另影響成本，不能把呼叫次數比例當作 FPS 改善。
- 桌機基本回歸：browser-report.json 10 組通過、錯誤 0，包含 11 素材模型、3 武器預覽、建造升級出售、切槍、暫停、連射、倍速、第二波與手機操作。
- 最終畫面：third-person-desktop.png、third-person-mobile.png、third-person-landscape.png；完整結果另有 full-victory.png、full-defeat.png。
- 測試網址：生產預覽 http://127.0.0.1:4173/ ，與開發 HMR 分開。實際遊玩使用 http://127.0.0.1:5173/ 或啟動程式顯示網址；兩來源的 localStorage 不共用。
- 限制：實體 iPhone／Android、Safari、低階裝置、長時間發熱、極慢／永不回應網路、真人難度／聽感／首次教學均未驗收。本機 rAF 取樣受其他程式負載影響，非 GPU 計時。
- 本次未安裝或移植 Godot，未新增 SKIN 選擇器、玩家傷害、多人、雲端、PWA 或公開部署。H:\zombie 無 Git 儲存庫／遠端，沒有 commit／push。

## v1.2.0：社群分享、更新通知與響應式發布

25 項單元測試、發布檢查通過。release-browser-report.json：首次安裝、真版本提示、延後去重、waiting 後重載、套用一次、連續更新、舊 CDN 拒絕、真實缺失 chunk 只恢復一次，以及七種 320～1440px 尺寸開波／切槍／設定皆通過，例外 0。手機橫向暫停按鈕曾上溢 0.5px，頂欄調至 48px 後修復；最終版權頁尾另測四尺寸可見且不擋操作。實體裝置及實際 LINE／FB 發文尚未測試。

網站： https://cagoooo.github.io/zombie/ ，GitHub Actions 部署設定已加入；正式 HTTP／線上遊玩查核結果會存於 artifacts/live-release-report.json。先前段落無 Git／PWA／公開網站的描述只代表舊版歷史。

### 正式 GitHub Pages 驗收

- 初次部署 commit 40068fa；Actions 34014366605 成功：https://github.com/cagoooo/zombie/actions/runs/34014366605 。
- https://cagoooo.github.io/zombie/ 為 HTTP 200，HTTPS enforced，Pages build_type=workflow。
- v1.2.0、build 1.2.0-b45c92969702，HTML 與 version.json 一致；SW scope 僅 /zombie/。
- 首頁、SVG／ICO／Apple 圖示、manifest、SW、version.json、四種 app icons 與 MK2 共 12 個端點皆 HTTP 200，MIME 正確。
- OG 公開讀取 HTTP 200，1734×907、3,013,346 bytes；SHA256 eb84a1c35448256b402467d7486e6d0aa96c0f839cb2183b781f44ffdd7cff50，與本機檔案一致。
- 正式站 11 模型、3 武器預覽；桌機建塔／開波／切槍／暫停恢復、390×844 手機與 1024×768 平板模擬開波／切槍／頁尾皆正常，JavaScript 例外 0。
- 證據 artifacts/live-release-report.json、live-desktop.png、live-390x844.png、live-1024x768.png。沒有實際向 LINE／Facebook 發文，亦未宣稱已驗收其平台快取；實體裝置仍待驗收。
