# GitHub Pages、社群分享與版本更新

版本：1.3.0。公開入口：[DEADZONE｜能量防線](https://cagoooo.github.io/zombie/)。儲存庫：[cagoooo/zombie](https://github.com/cagoooo/zombie)。

## 部署流程

main 的 push 或手動執行 Deploy DEADZONE workflow → Node 24 / npm ci → 29 項單元測試 → Vite build → 發布檢查 → GitHub Pages。Pages 必須使用 GitHub Actions，產物是 dist；不直接發布 Vite 原始 index.html。

BGM 放在 public/audio，從 document.baseURI 解析 /zombie/audio/ 路徑，檔名含版本。MP3 採瀏覽器一般串流與 HTTP 快取，不納入 SW 安裝預快取；避免 Range 206 寫入 Cache API 失敗，完整離線音樂下載未納入。

Vite 保留相對 base，模型、圖示、manifest、sw.js 均從目前子目錄讀取；只在生產版註冊 SW，本機 dev 不註冊。另以 /zombie/ 子路徑伺服器測試，避免把根目錄預覽成功當 Pages 子路徑成功。

## 社群分享

site.config.json 管理正式網址、標題、說明、OG 圖及更新摘要。Vite 在建置時寫入 canonical、og:type/title/description/url/image、圖片實際尺寸、locale、Twitter large image。這些存在原始 HTML，爬蟲不用執行 JavaScript。

OG 圖 public/og-deadzone-v1.png 是 AI 生成的宣傳插畫，並非遊戲實機截圖；1734×907 PNG，約 1.91:1。標題與核心內容在中央，分享圖 URL 自動附內容 SHA256 摘要，換圖時 URL 隨內容改變。FB／LINE 各自的預覽快取仍由平台控制；HTTP 與 meta 檢查通過不等於已實際發文或立即清除平台快取。

圖示使用深綠底、萊姆綠護盾與閃電，含 SVG／ICO 16/32/48、Apple 180、192／512 一般與 maskable PNG。npm run icons 可重建；圖示生成不依賴中文系統字型。OG PNG 直接納入版控，CI 不重繪中文。

## 更新機制與操作

每次建置從程式、模型、圖示、設定生成內容摘要 id，並把 package 版本、GitHub workflow run number、更新摘要同步注入 APP、sw.js、version.json。同一份程式重跑原 workflow 不改版本身分。GitHub run number 依序增加，避免舊 CDN 回應被視為較新版本。正式發行時更新 package 版本及 site.config.json 的 notes；不要把時間戳當新版。

- waiting SW 透過 GET_VERSION 回報；前端比較 APP／active／waiting 版本。
- 新版本通過閘門才顯示下方浮動卡片，含更新說明、「重新整理載入最新版」與「稍後」。
- 同版關閉後不因 focus／online／輪詢反覆提示；version.json 差異只觸發 SW 檢查。
- 按鈕在點擊當下重新取得 waiting，再送指定 id 的 SKIP_WAITING；controllerchange 只在使用者套用時重載一次。
- 另一分頁更新時，本分頁會提示，不強制打斷戰局。
- 更新沿用最近準備階段檢查點；建議先完成當波。localStorage 不會被一般更新清除。
- 更新機制不處理未回應 SW 的激進清除；10 秒仍未接管會保留戰局並顯示可重試訊息。

## 快取與故障恢復

HTML network-first，只存在當代 HTML cache；不放入 static precache。JS/CSS 保留 Vite hash，install 預存建置清單。模型等無 hash 資源 network-first。version.json 和 sw.js 不由 SW 提供舊快取。

Cache 名稱含專案路徑，清理只作用於本專案；保留一代舊資源供已開啟分頁使用，不刪除同網域其他專案 SW 或快取。只有已載入的資源可能提供離線備援，尚未承諾全模型離線下載與離線完整遊玩。

chunk 自癒與一般更新分開：只對指定載入錯誤或已確認 module 404，使用 sessionStorage 每分頁限制一次；只清本 scope 的 SW/cache，保留 localStorage。第二次失敗提供手動重試，不無限重整。

## 驗證

scripts/verify-release-browser.mjs 使用隔離本機 /zombie/，模擬三個版本、waiting 重載、延後去重、更新一次、舊 SW、真實缺失 chunk 及第二次失敗；另測 320×640、390×844、844×390、768×1024、1024×768、1180×820、1440×1000。

頁尾含 © 年份、Made with ❤️ by 阿凱老師、使用者指定教師頁、版本；手機縮短作品名稱，保留作者、年份與版本。橫向上欄增至 48px，避免 44px 觸控按鈕超出邊界。實體手機／平板、Safari 和主觀觸控仍需實機驗收。

參考：[Vite 官方部署](https://vite.dev/guide/static-deploy)、[GitHub Pages workflow](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)、[Open Graph Protocol](https://ogp.me/)、[MDN updateViaCache](https://developer.mozilla.org/en-US/docs/Web/API/ServiceWorkerRegistration/updateViaCache)。
